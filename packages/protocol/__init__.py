"""
Citnega protocol package — data models, callable hierarchy, events, interfaces.

This package has no implementation code. It defines the typed contracts that
all other packages depend on. It imports only stdlib and pydantic.
"""
